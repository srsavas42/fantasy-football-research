# Optional credentialed data sources. Never commit real keys; .env is ignored.
# Process environment variables take precedence; otherwise the project reads
# this Git-ignored file for provider credentials.

FFMODEL_CFBD_API_KEY=
FFMODEL_ODDS_API_KEY=
FFMODEL_CFBD_MONTHLY_LIMIT=1000

# Optional overrides
# FFMODEL_CACHE_DIR=C:\path\to\ffmodel-cache
# FFMODEL_HTTP_TIMEOUT=30
