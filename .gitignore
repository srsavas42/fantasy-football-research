venv
__pycache__
notebooks
utils.py
*.ipynb
.cache/
.env
*.egg-info/
build/
dist/
.pytest_cache/

# Fitted posteriors. Large binary build outputs — the manifest and metadata
# JSON beside them are small and are the provenance worth keeping in git.
artifacts/**/*.nc
