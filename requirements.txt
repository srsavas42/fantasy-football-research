# Core dependencies — prefer `pip install -e ".[dev]"` (see pyproject.toml).
pandas>=1.5
numpy>=1.24
nfl_data_py>=0.3.3
pyarrow>=12

# Bayesian modeling (Phase 3+): pip install -e ".[models]"
pymc>=5.10
arviz>=0.17

# Dev
pytest>=7.4

# Legacy scrapers (scripts/, combine/): pip install -e ".[scrape]"
requests>=2.28
beautifulsoup4>=4.12
lxml>=4.9
html5lib>=1.1
